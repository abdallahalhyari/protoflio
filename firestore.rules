rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Zero-trust default-deny security rule:
    // Prevents unauthenticated public read/write access to Firestore collections.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
